import * as appModel from "./app-model";

// Model Defination
export class DepreciationModel {
    hasLoaded?: boolean = false;
    valueObject?: {
    };
    entity?: {
        name?: string;
    };
}